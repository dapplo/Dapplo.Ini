; mergedapp.en-US.ini
; A single merged language file where the main keys and the [core] module section coexist.
; No dedicated mergedapp.core.en-US.ini exists, so the loader must fall back to
; the [core] section inside this main file.
WelcomeMessage=Welcome (merged)
ErrorTitle=Error (merged)
Save_Button=Save (merged)
Cancel_Button=Cancel (merged)

[core]
CoreTitle=Core Module (merged)
CoreStatus=Ready (merged)
